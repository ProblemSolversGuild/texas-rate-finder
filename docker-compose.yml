version: "3"
services:
  frontend:
    image: "node"
    user: "node"
    working_dir: /home/node/app
    environment: 
      - NODE_ENV=development
    volumes:
      - ./:/home/node/app
    ports:
      - "3001:3001"
    command: bash -c "export PORT=3001 && npm start"
